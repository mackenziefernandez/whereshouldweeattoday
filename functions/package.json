{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^5.8.1",
    "firebase-functions": "^0.8.1",
    "recipe-ingredient-parser": "^1.5.2"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^2.5.2"
  }
}
